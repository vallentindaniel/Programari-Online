Organizare baza de date  pentru programari online 

welcome.php
Introducere in tabel 'PROGRAMARI': SPITALE,SECTII,ZI,ORA(IN FUNCTIE DE CE SE AFLA IN BAZA DE DATE)
NUME,PRENUME,DATA NASTERII,TELEFON;


==> spitale , sectii , 

$spital, $sectie, $zi_sel, $luna_sel, $an_sel, 
date trimise get: prin  completare formular programare 
judete=1
&localitati=1
&spital=1
&sectii=1
&zi_sel=4
&nume=valentin
&prenume=daniel
&data_an=1905
&data_luna=Ianuarie
&data_zi=1
&nr_tel=0725539566



me.php
organizare tabel me

Spital, Secție, ora, zi/luna/an, nume, prenume, data nașterii, nr telefon șterge




if ($result->num_rows > 0){      // este minim o programare      

                   while ($row = $result->fetch_assoc()) {   
                                  // se selecteaza ultima ora la care sa efectuat o programare        
                   $ora_se = $row["ora_select"];
                   }

                   if ($zi_sel == date("d") and $ora_se < date("G", time())) { 
// este selectata ziua curenta si severifica daca  ultima ora din baza de date este mai mica decat ora curenta
                       $ora_sel = date("G", time()) + 2;
                    }
                    if ($zi_sel == date("d") and $ora_se > date("G", time())) {
                        $ora_sel = $ora_se + 1;
                      } 


                   }                                                          // echo date("G", time())             
                   else{                      // nu este nici-o programare
                    $ora_sel = 8;
                   }
















                   if ($err == 1) {                                     
  $orr = date("H") + 2;
  $sql = "DELETE FROM programari WHERE `ora_select` < '$orr' ";
      $result = $link->query($sql);
}









if ($row["zi_select"] <= ( date(d)*10 )/10 ) {      // sunt programari facute
                          $err = 1;
                        }
                        else{
                          $err = 0;
                        }